PUBLIC_API_URL=http://localhost:3000
PUBLIC_EXTERNAL_API_URL=http://localhost:3000
PUBLIC_APP_URL=http://localhost:5173
PUBLIC_WAPP=5493794182462

# SMTP de GoDaddy (main@caipi.app) para el envio de "Solicitud Alta Empresa"
# Host/puerto son fijos (GoDaddy Email Essentials/Professional); solo la
# contraseña es secreta y debe cargarse en Vercel / .env.local, nunca aca.
SMTP_PASSWORD=

# Generado automaticamente al conectar el Vercel Blob store del proyecto
# (ver "vercel blob create-store"); no hace falta setearlo a mano.
BLOB_READ_WRITE_TOKEN=
