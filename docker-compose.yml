version: '3.3'
services:
  caipi_app:
    build: .
    container_name: caipi_app
    networks:
      - caipi
    ports:
      - '4002:3000'
    restart: always
    env_file:
      - ./.env

networks:
  caipi:
    external: true
